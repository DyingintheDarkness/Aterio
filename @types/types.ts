export type FormType = {
  name: string;
  contact: string;
  membersCount: string;
  time: string;
};
export type ReservationType = {
  archived: boolean;
  contact: string;
  createdAt: string;
  id: string;
  membersCount: string;
  name: string;
  seated: boolean;
  time: string;
};
